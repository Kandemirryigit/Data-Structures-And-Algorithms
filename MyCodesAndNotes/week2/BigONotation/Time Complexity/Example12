// Outer loop → n
// Inner loop → 5 → constant
// Time complexity: O(n*5) = O(n)
// Space complexity → just sum and counters → O(1)

#include <stdio.h>

int main(){
    int n=10;
    for(int i=0;i<n;i++){
        for(int j=0;j<5;j++){
            print("hi");
        }
    }

}
